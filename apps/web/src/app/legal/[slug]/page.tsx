import { notFound } from 'next/navigation';
export const metadata = { title: 'Información de la boutique', robots: { index: false } };
export default async function Legal({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; if (!['privacidad','terminos'].includes(slug)) notFound(); return <div className="page-shell prose"><span className="eyebrow">INFORMACIÓN DE LA BOUTIQUE</span><h1>{slug === 'privacidad' ? 'Privacidad' : 'Términos y condiciones'}</h1><p>Esta tienda está en desarrollo. El catálogo es ilustrativo y no está habilitada para operaciones comerciales reales.</p><h2>Información pendiente de publicación</h2><p>Antes de abrir al público, el responsable de la tienda debe publicar su identificación, canales de atención y la política correspondiente a su operación. Este aviso describe el estado de desarrollo y no reemplaza la documentación comercial definitiva.</p>{slug === 'privacidad' ? <><h2>Datos en este entorno</h2><p>Las cuentas guardan nombre, correo electrónico y una contraseña cifrada mediante hashing. Las direcciones, pedidos y comprobantes se conservan en el servidor de desarrollo. Usa datos de prueba, evita subir documentos personales y solicita al responsable la eliminación del entorno cuando termine la validación.</p><p>La bolsa usa una cookie funcional. Los favoritos de visitantes se guardan en este navegador; al iniciar sesión se sincronizan con la cuenta.</p></> : <><h2>Compras de prueba</h2><p>Los pedidos creados aquí sirven para probar el flujo de compra. No transfieras dinero ni utilices comprobantes reales. Las políticas de envío, cambios, devoluciones y datos personales deben estar completas antes de activar compras reales.</p></>}</div>; }
