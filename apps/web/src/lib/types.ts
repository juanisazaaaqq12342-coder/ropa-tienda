export type User = { id: string; name: string; email: string; phone: string | null; role: 'CUSTOMER' | 'EMPLOYEE' | 'ADMIN' };
export type Category = { id: string; name: string; slug: string; image: string | null };
export type Variant = { id: string; sku: string; size: string; color: string; colorHex: string; stock: number };
export type Product = { id: string; slug: string; name: string; description: string; details: string; materials: string; care: string; price: number; compareAtPrice: number | null; featured: boolean; isNew: boolean; active: boolean; categoryId: string; category: Category; images: { id: string; url: string; alt: string; position: number }[]; variants: Variant[]; createdAt: string };
export type Cart = { id: string; items: { id: string; quantity: number; variant: Variant & { product: Product }; lineTotal: number }[]; subtotal: number; shipping: number; total: number; freeShippingThreshold: number };
export type ShopSettings = { brandName: string; shippingFee: number; freeShippingThreshold: number; contactEmail: string; contactPhone?: string; paymentMethods: { id: 'NEQUI' | 'DAVIPLATA'; name: string; enabled: boolean; number: string; holder: string; instructions: string }[]; demoMode: boolean; googleEnabled: boolean };
export type ProductList = { items: Product[]; total: number; page: number; limit: number; pages: number };
export type Address = { id?: string; label?: string; name: string; phone: string; line1: string; line2?: string; city: string; department: string; postalCode?: string };
export type Order = { id: string; number: string; userId: string; status: 'PENDING_PAYMENT' | 'CONFIRMED' | 'PREPARING' | 'SHIPPED' | 'DELIVERED' | 'CANCELLED'; paymentStatus: 'PENDING' | 'UNDER_REVIEW' | 'APPROVED' | 'REJECTED'; paymentMethod: 'NEQUI' | 'DAVIPLATA'; subtotal: number; shipping: number; total: number; address: Address; createdAt: string; items: { id: string; productName: string; productSlug: string; image: string; size: string; color: string; quantity: number; unitPrice: number }[]; proofs: { id: string; originalName: string; status: string; note: string | null; createdAt: string }[]; shipment: { carrier: string; trackingNumber: string | null; trackingUrl: string | null; shippedAt: string | null; deliveredAt: string | null } | null; events: { id: string; status: string; message: string; createdAt: string }[] };
